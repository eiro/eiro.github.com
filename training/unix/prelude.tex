% \usetheme{Goettingen}
% \usecolortheme{dove}
% \usetheme{Unistra} % procedures d'installation sur r/@work

% \usepackage{tikz,tikz-qtree,tikz-qtree-compat}

\usepackage{tikz}
\usetikzlibrary{positioning}
\usepackage{smartdiagram}
\usepackage{menukeys}
\usepackage{listings}

\lstdefinestyle{shared}{numbersep=20pt,}

\lstnewenvironment{zsh}{\lstset{
    frameround=tttt,
    basicstyle= \ttfamily,
    frame = single,
    escapeinside={<@}{@>},
    style=shared,
    gobble=4,
    }}{}

\hypersetup{colorlinks=true,
linkbordercolor=blue,linkcolor=green}

\newcommand{\prompt}{\textbf{\color{blue}zsh>}}
\newcommand{\important}[1]{\textbf{\color{red}#1}}
\newcommand{\stderr}[1]{\texttt{\color{red}#1}}

% nécessaire pour ne pas avoir de \begin direct dans le pandoc 
% sinon le md est ignoré jusqu'au \end
\newcommand{\newcol}{\begin{columns}}
\newcommand{\delcol}{\end{columns}}

\usepackage{multirow}
\usepackage{wasysym,marvosym}

\newcommand{\cmdAnatomy}[4]{\begin{tabular}{ l l l l l l } 
\texttt{print} & \texttt{-l}  &  \texttt{-O}    & \texttt{lorem}
       & \texttt{ipsum} & dolor \\
\cline{#1}#2\texttt{\textit{#3}}#4\\
\end{tabular} }

\newcommand{\cmdAnatomyWholeStory}{
    \only<1>{\cmdAnatomy{1-1}{}{commande}{&&&&&}}
    \only<2>{\cmdAnatomy{2-3}{&}{flags}{&&&&}}
    \only<3>{\cmdAnatomy{2-6}{&}{parametres}{&&&}}
}

\usepackage{newunicodechar}
\newunicodechar{☹}{\frownie{}}
\newunicodechar{☺}{\smiley{}}
\newunicodechar{♥}{\Heart}

% \def\stylesFileHierarchy{every node/.style={ rectangle,  draw, rounded corners=0.2cm },%
% also/.style={ rectangle,  draw, rounded corners=0.2cm, fill=gray!20 },%
% virtual/.style={ rectangle,  draw, rounded corners=0.2cm, fill=red!10}}

% \newcommand{\vimModes}{
% 
%     \tikz[layered layout]{
%         \node (cmd)    [rounded rectangle] {command};
%         \node (i)      [rounded rectangle] {insert};
%         \node (norm)   [rounded rectangle] {normal};
%         \draw (i) edge[->] (norm);
% 
%     }
%     
%     % \begin{tikzpicture}
%     %     \tikzset{
%     %             EdgeStyle/.style={->}
%     %     }
% 
%     %     \GraphInit[vstyle=normal]
%     %     \Verticles{cicle}{commande,insertion,normal}
% 
% 
%     % %     \node (cmd) {commande};
%     % %     \node (i) {insertion};
%     % %     \node (norm) {normal};
%     % %     \draw  
%     % \end{tikzpicture}
% }
% 
% 
% 
% 
% 
% \tikz {
% 
%     \node (i) {i}
%         child { node (ixn) {ixn} }
%         child { node (nxi) {nxi} };
% 
%     \node (i) {i}
%         child { node (ixn) {ixn} }
%         child { node (nxi) {nxi} };
% 
%     % \node () {
%     % }
%     % \node (n) {normal}
%     %     child {
%     %         node (nxi) {\keys{i}, \keys{a}, \keys{o}, ... }
%     %             child { node (i) {insertion} }
%     %     }
%     %     child { node (ixn) {\keys{\return}, \keys{\esc}} } 
%     %     ;
%     % \draw (i) -- (ixn)
%     % \node (n)  at (3,1) {commande};
%     % \node (nxi) at (3,3) 
%     % \node (xni) at (6,3) 
%     % \node (i) at (4,6) {insertion};
%     % %\node (n) at (2,2) {normal};
%     % \draw (n) -- (nxi) [->] -- (i);
%     % \draw (i) -- (xni) [->] -- (n);
% }
